import React from "react";

const Form = () => {
    return (
        <form>
            <input
                placeholder="Enter a Todo..."
                className="task-input"
                type="tex"
            />
        </form>
    );
};

export default Form;
